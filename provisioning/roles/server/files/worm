#!/bin/bash
# Historical training artifact: not a real worm.
# It is intentionally inert and exists only for forensic investigation.
while true; do
  ping -c 1 1.2.3.4 >/dev/null 2>&1
  sleep 30
done
